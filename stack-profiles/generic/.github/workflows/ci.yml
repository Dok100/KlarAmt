name: CI

on:
  push:
  pull_request:

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Projektstruktur prüfen
        run: make check
      - name: Secrets scannen
        run: make security
